---
workstation:
  hosts:
    localhost:
      ansible_connection: local
  vars:
    ssh_config_path: ~/.ssh/id_ed25519

laptop:
  hosts:
    10.0.0.79:
      ansible_connection: ssh

odroid:
  hosts:
    odroid
  vars:
    ssh_config_path: ~/.ssh/id_ed25519
